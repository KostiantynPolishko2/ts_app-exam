import React, { FC, ReactElement } from 'react';
import { SectionWrapper } from './Section.styled';

interface VinCodeProps {}

const VinCode: FC<VinCodeProps> = (): ReactElement => (
   <SectionWrapper>
      Section VinCode
   </SectionWrapper>
);

export default VinCode;
